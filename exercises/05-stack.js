/**
 * TODO: Write a function stack, which takes two arguments :
 * first -  array of functions
 * second - starting argument
 * and execute them one by one.
 * argument of each next function should be result of previous function or starting argument for the first function.
 *
 * function stack should returns result of the last function
 * TODO: Note that array should be reverted because of stack tackes last function as first!
 *
 * usage example
 * stack(
 *  [
   *  (arg) => { return arg + 1;},
   *  (arg) => { return arg + 2;}
   *  (arg) => { return arg + 3;}
   *  (arg) => { return arg + 4;}
 *  ],
 *  0
 * ) // 10
 * */

const stack = (array, startArg) => {};


module.exports = stack;
